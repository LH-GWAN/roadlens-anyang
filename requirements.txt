# RoadLens Anyang - 로컬 Streamlit 애플리케이션 의존성
# 개발/검증 환경: Python 3.12 (Python 3.11 에서도 동작)

streamlit>=1.50           # width="stretch" API 사용
pandas>=2.2
numpy>=1.26
geopandas>=1.0
shapely>=2.0
folium>=0.17
streamlit-folium>=0.22
scikit-learn>=1.5          # DBSCAN / HDBSCAN(1.3+ 내장) 군집화
opencv-python-headless>=4.9,<5.0   # 4.x 만 haarcascade XML 을 동봉함(얼굴 탐지에 사용)
Pillow>=10.0
onnxruntime>=1.18
ortools>=9.10
python-dotenv>=1.0
requests>=2.31
pytest>=8.0

# 선택 의존성: .pt(PyTorch) 가중치를 직접 쓸 때만 필요합니다.
# ONNX(best.onnx)만 사용한다면 설치하지 않아도 앱이 동작합니다.
# ultralytics>=8.2
